# ============================================================================
# CourseKit MCP Server - Environment Variables Template
# ============================================================================
#
# IMPORTANT SECURITY NOTES:
# 1. NEVER commit .env files to version control (already in .gitignore)
# 2. Store API keys securely - treat them like passwords
# 3. Use different keys for development, staging, and production
# 4. Rotate keys regularly and immediately if compromised
# 5. Grant minimum required permissions for each API key
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.template .env
# 2. Fill in your actual API keys below
# 3. Verify .env is listed in .gitignore
# 4. Test configuration: npm run config:example
#
# ============================================================================

# ----------------------------------------------------------------------------
# PROVIDER API KEYS
# ----------------------------------------------------------------------------

# Gamma AI API Key
# Get your key from: https://gamma.app/settings/api
# Required for: AI-powered presentation generation
# Permissions needed: content.generate, content.export
# Rate limits: 20/min, 500/hour, 5000/day (free tier)
GAMMA_API_KEY=

# Example (DO NOT use this - get your own):
# GAMMA_API_KEY=gamma_1234567890abcdef1234567890abcdef

# ----------------------------------------------------------------------------
# OPTIONAL: FUTURE PROVIDER KEYS
# ----------------------------------------------------------------------------
# Uncomment and fill in if you add these providers

# OpenAI API Key (for future AI content generation)
# OPENAI_API_KEY=

# Anthropic Claude API Key (for future AI content generation)
# ANTHROPIC_API_KEY=

# Google AI API Key (for future AI features)
# GOOGLE_AI_API_KEY=

# ----------------------------------------------------------------------------
# COURSEKIT CONFIGURATION (Optional)
# ----------------------------------------------------------------------------
# These override default settings - usually not needed

# Logging level: error, warn, info, debug
# COURSEKIT_LOG_LEVEL=info

# Default output directory (relative to project root)
# COURSEKIT_OUTPUT_DIR=.coursekit/implementations

# Default configuration directory
# COURSEKIT_CONFIG_DIR=./config

# Maximum file size for uploads (in bytes, default: 10MB)
# COURSEKIT_MAX_FILE_SIZE=10485760

# Request timeout (in milliseconds, default: 30000 = 30 seconds)
# COURSEKIT_TIMEOUT=30000

# Number of retries on failure (default: 2)
# COURSEKIT_MAX_RETRIES=2

# ----------------------------------------------------------------------------
# PROVIDER PREFERENCES (Optional)
# ----------------------------------------------------------------------------
# Override default provider selection

# Preferred presentation provider: slidev, powerpoint, gamma
# COURSEKIT_PREFERRED_PRESENTATION_PROVIDER=slidev

# Preferred document provider: markdown, docx, pdf
# COURSEKIT_PREFERRED_DOCUMENT_PROVIDER=markdown

# Preferred spreadsheet provider: xlsx
# COURSEKIT_PREFERRED_SPREADSHEET_PROVIDER=xlsx

# Enable/disable specific providers: true, false
# COURSEKIT_GAMMA_ENABLED=false
# COURSEKIT_SLIDEV_ENABLED=true
# COURSEKIT_POWERPOINT_ENABLED=true

# ----------------------------------------------------------------------------
# DEVELOPMENT & DEBUGGING (Optional)
# ----------------------------------------------------------------------------

# Enable debug mode (verbose logging)
# COURSEKIT_DEBUG=false

# Enable API request logging (CAUTION: may log sensitive data)
# COURSEKIT_LOG_API_REQUESTS=false

# Dry run mode (don't make actual API calls)
# COURSEKIT_DRY_RUN=false

# Enable experimental features
# COURSEKIT_EXPERIMENTAL=false

# ----------------------------------------------------------------------------
# DEPLOYMENT ENVIRONMENT (Optional)
# ----------------------------------------------------------------------------

# Environment: development, staging, production
# NODE_ENV=development

# Application port (if running web server)
# PORT=3000

# ----------------------------------------------------------------------------
# SECURITY SETTINGS (Optional)
# ----------------------------------------------------------------------------

# Enable rate limiting (recommended for production)
# COURSEKIT_ENABLE_RATE_LIMIT=true

# Enable input validation (always recommended)
# COURSEKIT_VALIDATE_INPUTS=true

# Enable path sanitization (always recommended)
# COURSEKIT_SANITIZE_PATHS=true

# ----------------------------------------------------------------------------
# VERIFICATION CHECKLIST
# ----------------------------------------------------------------------------
# Before using this configuration:
#
# [ ] Copied .env.template to .env
# [ ] Added required API keys (at minimum: GAMMA_API_KEY if using Gamma)
# [ ] Verified .env is in .gitignore
# [ ] Tested configuration: npm run config:example
# [ ] Confirmed API keys work: npm run test:config
# [ ] Secured .env file (chmod 600 .env on Unix systems)
# [ ] Documented where keys are stored for team members
# [ ] Set up key rotation schedule (recommended: every 90 days)
#
# ============================================================================
# END OF CONFIGURATION
# ============================================================================
